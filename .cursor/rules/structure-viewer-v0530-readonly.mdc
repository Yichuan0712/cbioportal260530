---
description: Do not modify the 5/30 structure-viewer sandbox archive
alwaysApply: true
---

# Read-only archive: structure-viewer-sandbox-v0530

`structure-viewer-sandbox-v0530/` is a **frozen snapshot** (2026-05-30). Treat it as read-only reference.
See `structure-viewer-sandbox-v0530/READONLY-ARCHIVE.md` for contents.

## Do not

- Create, edit, delete, or rename any file under `structure-viewer-sandbox-v0530/`
- Run refactors, copy-back scripts, or bulk fixes inside that directory
- Suggest copying changes from the live sandbox into v0530

## Do instead

- Make all structure-viewer work in `structure-viewer-sandbox/`
- Copy **from** v0530 **to** the live sandbox only when the user explicitly asks to restore/compare
- If the user needs archive content, read it for reference only
